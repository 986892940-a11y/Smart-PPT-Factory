# Smart PPT Factory 工作流程详解

## 📋 整体流程图

```
PDF文件 → parser.py → course.json → main.py → PPT文件
   ↓                      ↓            ↓
 提取文字            结构化数据      使用模板生成
```

---

## 🔄 详细工作流程

### 阶段1：数据准备

#### 输入方式A：使用PDF（推荐）

```bash
# 1. 将PDF放入data目录
data/高中语文_高一_2025寒假_小组课_张三.pdf

# 2. 运行解析器
python Smart_PPT_Factory/parser.py
```

**parser.py 做什么？**
1. 读取PDF文件，提取所有文字
2. 调用Google Gemini AI分析文字内容
3. 按照预定义的JSON结构提取信息：
   - 学习目标
   - 考情分析
   - 知识点（标题、内容、例题等）
   - 作业、测试等
4. 保存为 `data/course.json`

**输出示例：**
```json
{
  "lecture_title": "《红楼梦》助读",
  "learning_objectives": [
    "识记《红楼梦》作者作品相关常识",
    "理解《红楼梦》主要内容和艺术特色"
  ],
  "knowledge_points": [
    {
      "title": "《红楼梦》基础知识",
      "content": "详细内容...",
      "example_mother": "母题...",
      "example_variant": "变式..."
    }
  ]
}
```

#### 输入方式B：使用测试数据（快速测试）

```bash
# 生成测试数据
python Smart_PPT_Factory/test_main.py
```

**test_main.py 做什么？**
- 创建一个预定义的 `course.json` 文件
- 包含完整的示例数据
- 用于快速测试系统是否正常工作

---

### 阶段2：PPT生成

```bash
# 运行主程序
python Smart_PPT_Factory/main.py
```

#### main.py 的执行流程

**步骤1：加载数据**
```python
data = load_course_data()  # 读取 course.json
cover_info = get_cover_info()  # 从PDF文件名解析封面信息
```

**步骤2：加载模板**
```python
prs = Presentation(config.MASTER_TEMPLATE)
# 模板：data/FY26高一语文寒假志高班01 《红楼梦》助读的副本.pptx
```

**步骤3：删除模板中的预设幻灯片**
```python
# 模板文件有37页示例内容，全部删除
while len(prs.slides) > 0:
    删除第一页
```

**步骤4：创建幻灯片构建器**
```python
builder = SlideBuilder(prs)
# 这是一个封装了所有幻灯片操作的类
```

**步骤5：生成开场部分（6页）**
```python
1. create_cover_slide(builder, cover_info)           # 封面
2. create_course_system_slide(builder)               # 课程体系
3. create_lecture_title_slide(builder, title)        # 讲义标题
4. create_learning_objectives_slide(builder, objs)   # 学习目标
5. create_exam_analysis_slide(builder, analysis)     # 考情分析
6. builder.add_content(slide, class_intro)           # 课程导入（可选）
```

**步骤6：知识点循环（每个知识点7-8页）**
```python
for kp in knowledge_points:
    1. create_knowledge_section_title_slide()        # 切片标题
    2. create_knowledge_checklist_slide()            # 知识清单（填空）
    3. create_knowledge_lecture_slide()              # 知识点讲解
    4. create_discussion_slide()                     # 开口说（可选）
    5. create_example_slide()                        # 经典例题（母题）
    6. create_method_summary_slide()                 # 解题方法（可选）
    7. create_example_slide()                        # 变式练习（可选）
```

**步骤7：生成结束部分（6页）**
```python
1. create_section_title_slide("课堂总结")            # 总结标题
2. create_summary_slide()                            # 总结页
3. create_section_title_slide("出门测")              # 出门测标题
4. builder.fill_placeholders(slide, quiz)            # 出门测
5. create_homework_slide(builder, homework)          # 课后作业
6. create_ending_slide(builder)                      # 结束页
```

**步骤8：保存文件**
```python
prs.save(config.OUTPUT_PATH)
# 输出：Smart_PPT_Factory/output/Final_Courseware_Strict.pptx
```

---

### 阶段3：验证结果

```bash
# 运行验证脚本
python Smart_PPT_Factory/verify_output.py
```

**verify_output.py 做什么？**
- 读取生成的PPT文件
- 显示每一页的布局名称
- 显示每一页的文本内容预览
- 统计总页数和文件大小

---

## 🏗️ 核心组件详解

### 1. slide_builder.py - 幻灯片构建器

**SlideBuilder 类**
```python
class SlideBuilder:
    def __init__(self, prs):
        self.prs = prs  # Presentation对象
        self.slide_width = prs.slide_width
        self.slide_height = prs.slide_height
    
    # 核心方法：
    def create_slide(layout_index)        # 创建幻灯片
    def fill_placeholders(**kwargs)       # 填充占位符
    def add_textbox(...)                  # 添加文本框
    def add_background_image(...)         # 添加背景图
```

**15个专用创建函数**
```python
create_cover_slide()                      # 封面
create_course_system_slide()              # 课程体系
create_lecture_title_slide()              # 讲义标题
create_learning_objectives_slide()        # 学习目标
create_exam_analysis_slide()              # 考情分析
create_knowledge_section_title_slide()    # 知识点切片标题
create_knowledge_checklist_slide()        # 知识清单
create_knowledge_lecture_slide()          # 知识点讲解
create_discussion_slide()                 # 开口说
create_example_slide()                    # 经典例题
create_method_summary_slide()             # 解题方法总结
create_section_title_slide()              # 章节标题
create_summary_slide()                    # 总结页
create_homework_slide()                   # 作业页
create_ending_slide()                     # 结束页
```

### 2. layout_config.py - 布局配置

**LAYOUT_INDEX - 布局索引映射**
```python
LAYOUT_INDEX = {
    "首页": 0,
    "标题和内容": 1,
    "知识点切片标题": 20,
    "知识清单+笔记页": 24,
    "经典例题": 25,
    # ... 共26个布局
}
```

**CONTENT_TO_LAYOUT - 内容类型到布局的映射**
```python
CONTENT_TO_LAYOUT = {
    "封面": 13,              # 使用"自定义版式"
    "知识点切片标题": 20,    # 使用"知识点切片标题"
    "经典例题": 25,          # 使用"经典例题"
    # ...
}
```

**PPT_STRUCTURE - PPT结构定义**
```python
PPT_STRUCTURE = [
    {"type": "封面", "layout": 13, "required": True},
    {"type": "知识点切片标题", "layout": 20, "repeat": "per_knowledge_point"},
    # ...
]
```

### 3. config.py - 配置文件

```python
# API配置
API_KEY = os.getenv("GOOGLE_API_KEY")
TEXT_MODEL = "gemini-2.0-flash-exp"

# 路径配置
MASTER_TEMPLATE = "Smart_PPT_Factory/data/FY26高一语文寒假志高班01 《红楼梦》助读的副本.pptx"
OUTPUT_PATH = "Smart_PPT_Factory/output/Final_Courseware_Strict.pptx"
JSON_PATH = "Smart_PPT_Factory/data/course.json"
```

### 4. utils.py - 工具函数

```python
def parse_filename_to_json(pdf_path):
    """
    从PDF文件名解析信息
    输入：高中语文_高一_2025寒假_小组课_张三.pdf
    输出：{
        "subject": "语文",
        "grade": "高一",
        "season": "寒假",
        "teacher": "张三"
    }
    """
```

---

## 🎯 关键技术点

### 1. 占位符智能填充

**有占位符的布局（如"标题和内容"）**
```python
slide = builder.create_slide(1)  # 布局1：标题和内容
builder.fill_placeholders(
    slide,
    title="学习目标",
    content="• 目标1\n• 目标2"
)
```

**工作原理：**
1. 获取幻灯片的所有占位符
2. 按名称匹配（标题、内容、正文等）
3. 自动填充对应的文本

### 2. 无占位符布局处理

**无占位符的布局（如"经典例题"）**
```python
slide = builder.create_slide(25)  # 布局25：经典例题（无占位符）
builder.add_textbox(
    slide, "经典例题",
    left=0.5, top=0.5, width=6, height=0.8,
    font_size=32, bold=True
)
builder.add_textbox(
    slide, example_content,
    left=1, top=1.5, width=14, height=6.5,
    font_size=22
)
```

**工作原理：**
1. 检测布局是否有占位符
2. 如果没有，手动添加文本框
3. 指定文本框的位置、大小、样式

### 3. 知识点循环逻辑

```python
for i, kp in enumerate(knowledge_points, 1):
    # 每个知识点生成7-8页
    # 根据是否有可选内容（discussion, method等）决定页数
    
    # 必选页面
    create_knowledge_section_title_slide()
    create_knowledge_checklist_slide()
    create_knowledge_lecture_slide()
    
    # 可选页面（根据数据决定是否生成）
    if kp.get("discussion"):
        create_discussion_slide()
    
    if kp.get("example_mother"):
        create_example_slide()
    
    if kp.get("method"):
        create_method_summary_slide()
    
    if kp.get("example_variant"):
        create_example_slide()
```

---

## 📊 数据流转示意图

```
┌─────────────┐
│  PDF文件    │
│ (原始讲义)  │
└──────┬──────┘
       │
       ↓ parser.py (AI解析)
       │
┌──────┴──────┐
│ course.json │ ← 也可以手动编辑
│ (结构化数据) │
└──────┬──────┘
       │
       ↓ main.py (读取数据)
       │
┌──────┴──────────────────────┐
│ 1. 加载模板                  │
│ 2. 删除预设幻灯片            │
│ 3. 创建SlideBuilder          │
└──────┬──────────────────────┘
       │
       ↓ 循环生成
       │
┌──────┴──────────────────────┐
│ 开场部分 (6页)               │
│ ├─ 封面                      │
│ ├─ 课程体系                  │
│ ├─ 讲义标题                  │
│ ├─ 学习目标                  │
│ ├─ 考情分析                  │
│ └─ 课程导入                  │
└──────┬──────────────────────┘
       │
       ↓ for each knowledge_point
       │
┌──────┴──────────────────────┐
│ 知识点部分 (7-8页/个)        │
│ ├─ 切片标题                  │
│ ├─ 知识清单                  │
│ ├─ 知识点讲解                │
│ ├─ 开口说 (可选)             │
│ ├─ 经典例题 (可选)           │
│ ├─ 解题方法 (可选)           │
│ └─ 变式练习 (可选)           │
└──────┬──────────────────────┘
       │
       ↓
       │
┌──────┴──────────────────────┐
│ 结束部分 (6页)               │
│ ├─ 课堂总结标题              │
│ ├─ 课堂总结                  │
│ ├─ 出门测标题                │
│ ├─ 出门测                    │
│ ├─ 课后作业                  │
│ └─ 结束页                    │
└──────┬──────────────────────┘
       │
       ↓ prs.save()
       │
┌──────┴──────┐
│  PPT文件    │
│ (最终输出)  │
└─────────────┘
```

---

## 🔍 调试和分析工具

### analyze_ppt_logic.py - 模板分析工具

```bash
python Smart_PPT_Factory/analyze_ppt_logic.py
```

**功能：**
1. 分析模板PPT的所有布局
2. 显示每个布局的占位符信息
3. 逐页分析现有幻灯片的结构
4. 统计布局使用频率
5. 总结制作逻辑

**用途：**
- 了解模板结构
- 查看布局索引
- 分析占位符类型
- 学习PPT制作规律

---

## 💡 使用场景

### 场景1：快速测试系统

```bash
# 1分钟快速测试
python Smart_PPT_Factory/test_main.py    # 生成测试数据
python Smart_PPT_Factory/main.py         # 生成PPT
python Smart_PPT_Factory/verify_output.py # 验证结果
```

### 场景2：从PDF生成讲义

```bash
# 完整流程
1. 将PDF放入 Smart_PPT_Factory/data/
2. python Smart_PPT_Factory/parser.py
3. 检查 data/course.json，必要时手动调整
4. python Smart_PPT_Factory/main.py
5. 打开生成的PPT检查效果
```

### 场景3：手动编辑数据

```bash
1. 直接编辑 Smart_PPT_Factory/data/course.json
2. python Smart_PPT_Factory/main.py
3. 查看效果
```

### 场景4：分析新模板

```bash
# 如果要使用新的PPT模板
1. 将新模板放入 data/ 目录
2. 更新 config.py 中的 MASTER_TEMPLATE 路径
3. python Smart_PPT_Factory/analyze_ppt_logic.py
4. 根据分析结果更新 layout_config.py
5. 更新 slide_builder.py 中的创建函数
```

---

## ⚙️ 自定义和扩展

### 修改文本样式

编辑 `slide_builder.py` 中的 `add_textbox` 方法：

```python
def add_textbox(self, slide, text, left, top, width, height, 
                font_size=28,           # 修改默认字号
                font_name="微软雅黑",    # 修改字体
                bold=False,             # 是否加粗
                color=(0, 0, 0),        # RGB颜色
                align="left"):          # 对齐方式
```

### 添加新的页面类型

1. 在 `layout_config.py` 中添加布局映射
2. 在 `slide_builder.py` 中添加创建函数
3. 在 `main.py` 中调用新函数

### 修改生成流程

编辑 `main.py` 中的 `generate_ppt()` 函数，调整页面顺序或添加新页面。

---

## 🎓 总结

**核心理念：**
1. **数据驱动** - 所有内容来自 course.json
2. **模板复用** - 使用实际的PPT模板文件
3. **模块化** - 每个功能独立封装
4. **可扩展** - 易于添加新功能

**工作流程：**
```
数据准备 → 加载模板 → 循环生成 → 保存文件
```

**关键文件：**
- `main.py` - 主流程控制
- `slide_builder.py` - 幻灯片操作
- `layout_config.py` - 布局配置
- `course.json` - 数据源

现在你可以告诉我具体有什么问题需要修改了！
